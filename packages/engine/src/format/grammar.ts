/** The fixed strings the task file format is built from. */

export const FRONTMATTER_DELIMITER = "---";

export const MARKER_PREFIX = "<!-- task:comment";

/** The sequence that closes an HTML comment, and with it a marker. */
export const MARKER_SUFFIX = "-->";
