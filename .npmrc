# pnpm honours `registry` only from an npmrc file: the same key in
# pnpm-workspace.yaml is reported by `pnpm config get` but ignored when
# resolving. Auth tokens never belong here — keep them in ~/.npmrc.
registry=https://registry.npmjs.org/
