node_modules/
dist/
coverage/
*.log
.DS_Store
.playwright-cli/

# Local planning and workflow content, never part of the repository
.specs/
CLAUDE.local.md

# Environment and secret files
.env
.env.*
!.env.example
*.env
.envrc
*.pem
*.key
*.p12
*.pfx
*.p8
id_rsa
id_ecdsa
id_dsa
id_ed25519

# Nested npmrc files may hold auth tokens; the root one pins the registry only
.npmrc
!/.npmrc

# Rust build output and the schemas tauri_build generates, named in full so
# neither can hide a directory of that name elsewhere.
apps/macos/target/
apps/macos/gen/

# The compiled daemon and the compiled CLI the application ships beside its own
# executable, built by scripts/app-binaries.sh and about 128 MB per architecture.
apps/macos/binaries/
